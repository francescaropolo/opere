import React from 'react';
import PropTypes from 'prop-types';

const Opera = props => {
    return (
        <div>
            opera
        </div>
    );
};

Opera.propTypes = {

};

export default Opera;